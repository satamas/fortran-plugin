! Note: test now is accepted but really works bad
! We must remove 'cap' from the grammar!!!
program a
  if (a + x > 0) b = 1
  if (.not.c) then
    d = 3.1
  elseif (d .and. d) then
    f = 14
  endif
  if (.true.) then
    n = .false.
  else if (n .or. .true.) then
    l = 1
  else
    k = 1
  endif
endprogram
